// src/pages/organizer/config.js
/**
 * Configuration flags for the Organizer Dashboard.
 *
 * `SHOW_ENGAGEMENT_ANALYTICS`:
 * Set to `false` to temporarily hide low-activity engagement metrics
 * (Total Registrations, Conversion Rate, Total Event Views, Verified Student Signups,
 * and views/registration comparison charts).
 *
 * To re-enable in the future when FestNest reaches higher user volume,
 * simply change this flag to `true`.
 */
export const SHOW_ENGAGEMENT_ANALYTICS = false;
